.sidebar
    .sidebar-logo
       a.sidebar-link SDS
    mixin list(classname, ...items)
        ul(class= ""+classname)
          each item in items
            li
              a(class="page-nav__link")= item

    +list("page-nav","Work","About","Services", "Blog","Contact")

    button.sidebar__search-btn
       i(class="fas fa-search")
